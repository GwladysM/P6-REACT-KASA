function TitleLocation({ title, location }) {
    return (
        <div className="logement__title">
            <h2>{title}</h2>
            <h3>{location}</h3>
        </div>
    )
}

export default TitleLocation